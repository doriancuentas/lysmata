# CLAUDE.md

## Conventions
- **Bash scripts**: `show_usage()` IS the documentation. No separate comments. See `check.sh`.
- **Workflow**: Run `./check.sh` → fix issues → commit. Keep README, CHANGELOG, NEXTSTEPS slim.
