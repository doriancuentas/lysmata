# Lysmata - Lint and Security Tools
# Install: pip install -r .laf/requirements.txt

# Python linting + formatting (replaces black, isort, flake8, pylint)
ruff>=0.8.0

# Security: static code analysis (replaces bandit - more modern, better rules)
semgrep>=1.0.0

# Security: dependency vulnerability scanner (replaces safety - from PyPA, more accurate)
pip-audit>=2.0.0

# YAML linting
yamllint>=1.35.0

# HTML template linting (works with Django, Jinja2, etc.)
curlylint>=0.13.0
